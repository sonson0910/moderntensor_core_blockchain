const { ethers } = require("hardhat"); async function main() { const contractAddress = "0xAA6B8200495F7741B0B151B486aEB895fEE8c272"; const ModernTensor = await ethers.getContractFactory("ModernTensor"); const contract = ModernTensor.attach(contractAddress); console.log("📋 Checking contract requirements..."); const subnet0 = await contract.subnetStatic(0); console.log("Subnet 0 info:", { netUid: subnet0.net_uid.toString(), minStake: ethers.utils.formatEther(subnet0.min_stake_miner), registrationOpen: subnet0.creation_time > 0 }); const subnetDynamic = await contract.subnetDynamic(0); console.log("Registration open:", subnetDynamic.registration_open); } main().catch(console.error);
